# Environment knobs. $HAL_DATA_DIR (env var) always wins over anything here.
hf_repo: agent-evals/hal_traces
first_slice: taubench_airline        # 47 files, 3.41 GB — under the 5 GB bar
schema_sample_traces: 40             # N traces walked by discover_schema()
download_ask_threshold_gb: 5         # CLAUDE.md guardrail 6
