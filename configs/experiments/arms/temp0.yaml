# Temperature-0 sensitivity arm (§4.6): greedy decoding, K=3, one Tier-F
# model, headline configs — do component effects depend on sampling
# stochasticity? (submit BENCH=hotpotqa,math)
exp_id: arm_temp0
benchmark: hotpotqa          # overridden per slice
model_id: mistral_small_3_2_24b
tasks_file: configs/tasks/hotpotqa.jsonl   # overridden per slice
n_tasks: 100
k_seeds: 3
temp: 0.0
top_p: 1.0
schedule_seed: 20260725
concurrency: 16
throughput_rollouts_per_node_hour: 4000
configs:
  - components: []
  - components: [T]
  - components: [T, SR, R]
  - components: [P, T, M, SR, R]
