# environments & secrets
.venv/
vendor/
.env

# raw rollout text NEVER enters git (results contract, CLAUDE.md rule 3);
# the pre-commit guard (scripts/hooks/pre-commit) enforces this too
**/rollouts/

# legacy observational arm: gitignored data symlink on the cluster
data
data/

# python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# notebooks
.ipynb_checkpoints/

# os
.DS_Store
