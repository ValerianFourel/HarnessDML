# Copy to .env (gitignored). Secrets never in code or configs (CLAUDE.md rule 7).

# Blablador (Phase 2 live smoke) — OpenAI-compatible API key
# https://helmholtz-blablador.fz-juelich.de
BLABLADOR_API_KEY=

# Hugging Face token — model prefetch on JUPITER login nodes only
HF_TOKEN=
